% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/250813_eCOMET_build_V3.R
\name{GetFunctionalHillNumber_derep}
\alias{GetFunctionalHillNumber_derep}
\title{GetFunctionalHillNumber}
\usage{
GetFunctionalHillNumber_derep(
  feature,
  metadata,
  distance_matrix,
  q = 1,
  threshold = 0,
  scale_dissim = TRUE
)
}
\arguments{
\item{feature}{Feature table with columns: id, feature, then sample columns}

\item{metadata}{Metadata table with sample and group columns}

\item{distance_matrix}{Feature distance matrix}

\item{q}{The order of the Hill number to calculate (default: 1).
Larger q values give more weight to evenness portion of the hill number over richness.
q = 0 treats abundance matrix as a presence absence matrix}

\item{threshold}{Numeric; detection threshold for presence (default: 0)}

\item{scale_dissim}{Boolean; whether to scale the distance matrix to be between 0 and 1 (default: TRUE)}
}
\value{
A data frame containing the functional Hill number for each group in the metadata, with columns for group and hill number.
}
\description{
This function calculates the functional Hill number for a given \code{mmo}, normalization method, and distance metric.
See https://nph.onlinelibrary.wiley.com/doi/full/10.1111/nph.18685 for details of the functional Hill number calculation.
}
\section{Lifecycle}{

\code{GetFunctionalHillNumber_derep()} is deprecated. It reads the dissimilarity (\code{.dissim}) slots written
by \code{\link[=AddChemDist]{AddChemDist()}} / \code{\link[=AddCustomDist]{AddCustomDist()}}, the storage layout that
\code{\link[=AddChemSim]{AddChemSim()}} and the \code{.sim} slots replaced. Use \code{\link[=GetFunctionalHillNumber]{GetFunctionalHillNumber()}} instead;
corrections made after the change in storage were applied there, so the two
can return different numbers. Calling this function raises a deprecation
warning; silence it with \code{options(lifecycle_verbosity = "quiet")}.
}

